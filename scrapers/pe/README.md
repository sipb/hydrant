# PE Subjects

For every quarter, add a file in this folder named `pe-q#.csv`, where `#` is the relevant quarter number. To add an override, also add a file called `pe-q#-overrides.toml` and structure it similarly to other override TOML files.
